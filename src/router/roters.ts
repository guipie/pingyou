export enum RoutersName {
  Home = "home",
  NotFound = "not-found",
  ProviderAdd = "provider-add",
}

export enum ContainerRouters {
  chatModelSetting = "chatModelSettings",
  providerOllamaSetting = "providerOllamaSetting",
  notFound = "notFound",
}
